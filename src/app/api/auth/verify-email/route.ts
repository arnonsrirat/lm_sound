import { NextRequest, NextResponse } from "next/server";
import crypto from "node:crypto";
import { prisma } from "@/lib/prisma";
export async function POST(request: NextRequest) { const body = await request.json() as { email?: string; code?: string }; const email = body.email?.trim().toLowerCase(); if (!email || !body.code) return NextResponse.json({ success: false, error: "กรุณากรอกอีเมลและรหัสยืนยัน" }, { status: 400 }); const user = await prisma.user.findUnique({ where: { email } }); if (!user) return NextResponse.json({ success: false, error: "ไม่พบผู้ใช้นี้" }, { status: 404 }); const hash = crypto.createHash("sha256").update(body.code).digest("hex"); const token = await prisma.authToken.findFirst({ where: { userId: user.id, purpose: "EMAIL_VERIFY", tokenHash: hash, expiresAt: { gt: new Date() } } }); if (!token) return NextResponse.json({ success: false, error: "รหัสไม่ถูกต้องหรือหมดอายุ" }, { status: 400 }); await prisma.user.update({ where: { id: user.id }, data: { emailVerifiedAt: new Date() } }); await prisma.authToken.delete({ where: { id: token.id } }); return NextResponse.json({ success: true, message: "ยืนยันอีเมลสำเร็จ สามารถเข้าสู่ระบบได้" }); }
