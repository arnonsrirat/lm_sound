"use client";
import { useState } from "react";
import { notify } from "@/lib/notify";
export default function VerifyEmailPage() { const [email, setEmail] = useState(""); const [code, setCode] = useState(""); const [loading, setLoading] = useState(false); const submit = async () => { setLoading(true); const r = await fetch("/api/auth/verify-email", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email, code }) }); const d = await r.json(); setLoading(false); if (!r.ok) { notify(d.error, "error"); return; } notify(d.message, "success"); window.location.href = "/login"; }; return <main className="flex min-h-screen items-center justify-center bg-background px-4"><div className="w-full max-w-md rounded-3xl border border-purple-500/30 bg-[var(--card-bg)] p-6 shadow-2xl"><h1 className="text-2xl font-black">ยืนยันอีเมล</h1><p className="mt-2 text-sm text-foreground/60">กรอกรหัส 6 หลักที่ส่งไปยังอีเมลของคุณ</p><input type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="อีเมล" className="mt-5 w-full rounded-xl border border-purple-500/25 bg-purple-950/30 px-4 py-3 text-sm" /><input inputMode="numeric" value={code} onChange={(e) => setCode(e.target.value)} placeholder="รหัสยืนยัน" className="mt-3 w-full rounded-xl border border-purple-500/25 bg-purple-950/30 px-4 py-3 text-sm" /><button disabled={loading} onClick={() => void submit()} className="mt-4 w-full rounded-xl bg-fuchsia-500 py-3 text-sm font-bold text-white">{loading ? "กำลังตรวจสอบ..." : "ยืนยันอีเมล"}</button></div></main>; }
