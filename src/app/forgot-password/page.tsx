"use client";
import { useState } from "react";
import Link from "next/link";
import { notify } from "@/lib/notify";
export default function ForgotPasswordPage() { const [email, setEmail] = useState(""); const [code, setCode] = useState(""); const [password, setPassword] = useState(""); const [sent, setSent] = useState(false); const [loading, setLoading] = useState(false); const submit = async (action: "request" | "reset") => { setLoading(true); const response = await fetch("/api/auth/password-reset", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ action, email, code, password }) }); const data = await response.json(); setLoading(false); if (!response.ok) { notify(data.error || "ทำรายการไม่สำเร็จ", "error"); return; } if (action === "request") { setSent(true); notify("ส่งรหัสยืนยันไปยังอีเมลแล้ว", "success"); } else { notify("ตั้งรหัสผ่านใหม่สำเร็จ", "success"); window.location.href = "/login"; } }; return <main className="flex min-h-screen items-center justify-center bg-background px-4"><div className="w-full max-w-md rounded-3xl border border-purple-500/30 bg-[var(--card-bg)] p-6 shadow-2xl sm:p-8"><Link href="/login" className="text-xs text-purple-300">← กลับหน้าเข้าสู่ระบบ</Link><h1 className="mt-5 text-2xl font-black">ลืมรหัสผ่าน</h1><p className="mt-2 text-sm text-foreground/60">กรอกอีเมลเพื่อรับรหัสยืนยัน 6 หลักทางอีเมล</p><input type="email" required value={email} onChange={(e) => setEmail(e.target.value)} placeholder="อีเมลของคุณ" className="mt-5 w-full rounded-xl border border-purple-500/25 bg-purple-950/30 px-4 py-3 text-sm" /><button disabled={loading} onClick={() => void submit("request")} className="mt-3 w-full rounded-xl bg-fuchsia-500 py-3 text-sm font-bold text-white disabled:opacity-50">ส่งรหัสยืนยัน</button>{sent && <div className="mt-5 space-y-3 border-t border-purple-500/20 pt-5"><input inputMode="numeric" value={code} onChange={(e) => setCode(e.target.value)} placeholder="รหัสยืนยัน 6 หลัก" className="w-full rounded-xl border border-purple-500/25 bg-purple-950/30 px-4 py-3 text-sm" /><input type="password" minLength={8} value={password} onChange={(e) => setPassword(e.target.value)} placeholder="รหัสผ่านใหม่อย่างน้อย 8 ตัว" className="w-full rounded-xl border border-purple-500/25 bg-purple-950/30 px-4 py-3 text-sm" /><button disabled={loading} onClick={() => void submit("reset")} className="w-full rounded-xl border border-cyan-400/40 bg-cyan-500/15 py-3 text-sm font-bold text-cyan-200">ยืนยันและเปลี่ยนรหัสผ่าน</button></div>}</div></main>; }
